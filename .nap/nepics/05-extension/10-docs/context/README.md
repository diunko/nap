# extension context — required reading

Read in this order.

## 1. Why and what

- `02-app-ux.nap.md` (parent dir) — the UX mental model: navigation/map/territory, how the app works, why mini-books exist, where this is going

## 2. The vision

- `10-du-thoughts.nap.md` — DU's thinking on workflowy mode, decorations, diff layers, collaboration. This is where the extension evolves. Don't make decisions that close off these paths.

## 3. The v0 scope

- `33-v0.nap.md` — refined v0 scope: three surfaces (terminal, editor, file tree), shared LightningFS, git flow via terminal

## 4. The workflow

- `02-workflow.nap.md` — how the reviewer enters (link with #fragment), state-key per PR, auto-clone, napkin focus, fetch latest

## 5. The approved design

- `design-spec.nap.md` — every design decision: layout, nav card system, agent dots, tab bar, editor tokens, terminal dark theme, resize handle
- `mock-e-screenshot.png` — what it looks like (side by side with GitHub)
- Mock HTML: `.nap/nepics/05-extension/30-napkins/0200-design-sprint/mocks/mock-e.html` — open in browser
