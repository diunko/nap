{
  "name": "nap-extension",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build && cp manifest.json dist/manifest.json",
    "typecheck": "tsc --noEmit",
    "test:small": "vitest run --reporter=verbose",
    "test:small:watch": "vitest --reporter=verbose",
    "test:medium": "playwright test",
    "test:medium:headed": "playwright test --headed"
  },
  "dependencies": {
    "@isomorphic-git/lightning-fs": "^4.6.2",
    "@wterm/core": "^0.3.0",
    "@wterm/dom": "^0.3.0",
    "buffer": "^6.0.3",
    "isomorphic-git": "^1.38.0",
    "just-bash": "^3.0.1",
    "monaco-editor": "^0.52.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "typescript": "^5.5.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
