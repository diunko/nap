{
  "extends": "../tsconfig.json",
  // TODO: restore after fixing pre-existing type errors in all test files:
  // "include": ["**/*.ts", "../playwright.config.ts", "../vitest.config.ts", "../src/types/**/*.d.ts"],
  "include": [
    "live-wiring/**/*.ts",
    "../playwright.config.ts",
    "../vitest.config.ts",
    "../src/types/**/*.d.ts"
  ],
  "compilerOptions": {
    "types": ["vitest/globals"]
  }
}
